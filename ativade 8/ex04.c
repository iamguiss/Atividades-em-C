#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    srand(time(NULL));
    int sorteio = rand() % 100 + 1;
    int contador = 1;

    while (sorteio)
    {
    }

    return 0;
}
